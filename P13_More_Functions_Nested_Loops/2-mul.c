#include <stdio.h>
#include "main.h"
/**
 * mul - multiplu two integers
 * @a: The first integer.
 * @b: the second integer
 *
 * Return: Always 0 (Success)
 */

int mul(int a, int b)
{
	return (a * b);
}
